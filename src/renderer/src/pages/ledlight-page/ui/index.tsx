import { FC, PropsWithChildren } from 'react'

export const LedlightPage: FC<PropsWithChildren<{ className?: string }>> = ({
  className
}) => {
  return <div className={className}>LedlightPage</div>
}
